# TypeScript/JavaScript linting
npm run lint

# Kotlin linting (if ktlint is available)
if command -v ktlint &> /dev/null; then
  echo "Running ktlint on Kotlin files..."
  ktlint "android/src/**/*.kt" --format || exit 1
else
  echo "ktlint not installed, skipping Kotlin lint"
fi
